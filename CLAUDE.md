# Tessera — project memory

## What this is
Tessera is a study-organization platform built around web highlighting: highlight text/images/screenshot regions on any web page, save with a deep-link anchor back to the exact spot. Snippets land in a cloud-synced library auto-grouped by source site, curatable into custom **documents** mixing material from many sites. AI study tools (summaries, grounded Q&A with citations, flashcards, quizzes) run over the user's own saved material.

- **Audience (v1):** students/self-learners; small private beta. No billing, no collaboration, Chrome-only capture.
- **Spec:** `docs/Tessera-PRD-v0.1.md` — requirements locked, founder decisions in §0 (do not re-litigate without cause). Roadmap M0–M5 in §11.
- **Standalone project.** `../luminote` is a separate, older project of the founder's (similar in-browser highlighting). **Do not use it as a reference** — founder's call: it's stale and not a good reference. Build capture/anchoring fresh.

## Architecture & stack
- TypeScript end-to-end; npm workspaces monorepo (Node ≥20).
- `packages/core` — `@tessera/core`: shared domain types, ids, anchor utils, **fractional-index ordering** (`src/types.ts`, `ids.ts`, `anchor.ts`, `order.ts` — `keyBetween`/`keyAfter`/`keyBefore` for `document_items.position`, decimal-fraction keys, lexicographic == numeric order), and **document export** (`export.ts` — pure `documentToMarkdown` + `buildTextFragmentUrl`, the shared `#:~:text=` deep-link builder; tested in `export.test.ts`).
- `packages/db` — `@tessera/db`: local-first store + sync engine. `LocalStore`/`RemoteAdapter` contracts, pure LWW+tombstone reconcile (`reconcile.ts`), `SyncEngine` (outbox push + cursor delta-pull), `DexieLocalStore`, `SupabaseRemoteAdapter`, in-memory fakes. **Client-authored timestamps** (server preserves them) → "last edit wins". M3 hardening: `SyncEngine.push` drains tables in the **declared `tables` order** (parents first — `snippets`/`tags`/`documents` before `snippet_tags`/`document_items`) so cross-table FKs resolve, and push/pull is **error-isolated at the row level** (M4: a batch the server rejects for one bad row is retried **row-by-row**, so only the poison row stays queued and retries — never starving its siblings, other tables, or the pull phase; the run still surfaces the error). `SyncEngine.pendingCount()` exposes how many changes are still queued (offline / server-rejected) for the UI. Dexie schema is **v2** (adds `documents`/`document_items`/`tags`/`snippet_tags`). See `packages/db/README.md`.
- `apps/extension` (M1) — Chrome MV3 via Vite + `@crxjs/vite-plugin` + React + Tailwind. `src/content/content.ts` (selection → floating Save → `anchorFromRange` → message background; re-highlight on load via `resolveAnchor` + CSS Custom Highlight API), `src/background/service-worker.ts` (**owns the Dexie store + Supabase session**: persists captures, serves snippets-by-url, **email/password auth + `SyncEngine` cloud sync** — session in `chrome.storage` via `src/lib/supabase.ts`, claims `local` captures on sign-in; context menu, periodic alarm), `src/popup` (React auth UI + snippet list). Store lives in the background because a content script sees the *page's* IndexedDB, not the extension's. Builds to loadable unpacked `dist/`. `tsconfig` extends base (Bundler resolution, not NodeNext).
- `apps/web` (M2) — `@tessera/web`: Next.js 14 App Router + Tailwind, the synced **library platform**. A local-first SPA reusing `@tessera/core`/`@tessera/db` verbatim: browser Supabase session (`src/lib/supabase.ts` — magic-link + email/password, PKCE, `detectSessionInUrl`, localStorage; no SSR/cookie auth), Dexie reads via `dexie-react-hooks` `useLiveQuery` (`src/lib/hooks.ts`) + a `SyncEngine(['snippets'])` built on sign-in and run on load/interval/focus/online (`src/components/providers.tsx`). Routes (`src/app`): `/` browse-by-website (or flat results when searching/filtering), `/site/[domain]` page drill-down, `/snippet/[id]` detail, **`/documents` list + `/documents/[id]` editor (M3)**. Pure logic (grouping/filter/sort/`#:~:text=` deep-link/format) in `src/lib/snippets.ts`; private `snippet-images` rendered via signed URLs (`src/components/library/media.tsx`). **M3 documents** (PRD §7.4/§7.5): `src/lib/documents.ts` (create/rename/delete doc, add snippet-ref, add/edit/remove heading+text blocks, `reorderItem` via `keyBetween`) + `src/lib/tags.ts` (create/attach/detach, name-deduped) + `updateSnippet` in `src/lib/db.ts` (note/color/text, sets `edited`); live-query hooks in `src/lib/hooks.ts` (`useDocuments`/`useDocument`/`useDocumentItems`/`useDocumentsForSnippet` (where-used)/`useSnippetRefCounts`/`useTags`/`useSnippetTagMap`); UI in `src/components/documents/*` (list+new-doc dialog, editor with native HTML5 drag-handle reorder **+ up/down buttons** for a11y, snippet-picker + add-to-document dialogs). Snippet detail gained note/tag/color/text editing, "Add to document", and "Where used". Library wired the deferred M2 bits — **tag filter (LIB-4)** + **most-referenced sort (LIB-7)**. Lean `Dialog`/`Textarea` added to `ui.tsx`. **M4 export (PRD §7.6, EXP-1..3):** an **Export** dropdown in the editor toolbar (`ExportMenu` in `document-editor.tsx`) → **Markdown** downloads a slugified `.md` via `src/lib/export.ts` (`downloadDocumentMarkdown`, built on core's `documentToMarkdown`); **PDF/Print** opens `/documents/[id]/print` — a chrome-free, auth-gated print view (`components/documents/document-print-view.tsx`, no `AppShell`) with `@media print` rules in `globals.css`, where the browser's "Save as PDF" does the rest. Both carry per-snippet provenance (source deep-link + domain + saved date); `buildSourceUrl` now delegates to core's `buildTextFragmentUrl`. **Account (PRD §8.3):** `/account` (nav link in `app-shell.tsx`) — `src/lib/account.ts` does a one-click **data export** (live mirror → portable JSON, `downloadAccountExport`) and an irreversible **account deletion** behind a typed "DELETE" confirm (`deleteAccount`: best-effort Storage image removal → `delete_account` RPC → `clearLocalData()` wipes the local Dexie/outbox → sign out). **Onboarding (ON-2/ON-3):** a dismissible **Getting started** checklist on the library home (`src/lib/onboarding.ts` pure `onboardingProgress` + `components/library/onboarding-checklist.tsx`) walks capture → create-a-document → add-a-snippet-to-it, ticking steps off live from the user's own data and auto-hiding once complete or dismissed (localStorage); teaching empty states (`LibraryEmpty`/`DocumentsEmpty`) round out ON-3. Env from the **repo-root `.env`** loaded by `next.config.mjs` (no separate web `.env`). Lean shadcn-style primitives in `src/components/ui.tsx` (not full shadcn/ui yet). `next build`/`lint`/`typecheck` green; `eslint.ignoreDuringBuilds` (root flat ESLint owns linting). Dev server: `.claude/launch.json` "web" entry (port 3001).
- `supabase/migrations/` — Postgres schema as versioned, `supabase db push`-able migrations (RLS + pgvector + FTS). `0001_init` tables: user_settings, provider_keys, snippets, tags, snippet_tags, documents, document_items, snippet_embeddings, ai_artifacts, flashcards — plus `sync_push(_table,_rows)` (conditional LWW upsert) and a `set_updated_at` trigger that honors client-supplied `updated_at`. `0002` = `snippet-images` private Storage bucket + per-user RLS. `0003` adds the snippet `edited` flag (M3). `0004` adds the `delete_account()` **SECURITY DEFINER** RPC — deleting the auth user cascade-purges every table (account deletion, PRD §8.3; storage objects are cleared client-side first since Supabase blocks direct SQL deletes from `storage.objects`).
- Local-first: IndexedDB (Dexie) mirror + sync engine, last-write-wins per record + tombstones (no CRDT field merge in v1).
- Auth: magic-link + email/password fallback. AI: provider-agnostic (Claude/GPT/Gemini), BYOK in v1. Images/screenshots copied into user storage for durability.
- Documents use a **reference model** (items point at snippets; PRD §5).

## Status (as of 2026-06-09) — M0–M3 complete; **M4 complete** (export · account · onboarding · reliability)
- Done: monorepo + `@tessera/core`; Supabase migrations (RLS + `sync_push` + Storage bucket); dev tooling (ESLint/Prettier/Vitest) + CI (`.github/workflows/ci.yml`); **`@tessera/db` sync engine** — LWW + tombstones, 14 in-memory tests **+ verified end-to-end against local Supabase** (`sync-smoke.mjs`: create / edit-LWW / delete-tombstone through real Postgres + `sync_push` + RLS, smoke PASSED). Build/typecheck/lint/test green.
- Local backend: `supabase start` (CLI v2.x — Mailpit emails at :54324, new `sb_publishable_`/`sb_secret_` keys); `.env` populated with local values (git-ignored). Hosted is a `.env` swap away when beta-ready (PRD §13; founder is local-first/self-host-curious).
- M1 (in progress): **anchoring DONE** (`@tessera/core` `anchorFromRange`/`resolveAnchor` — text-quote + prefix/suffix + text-position fallback; 7 happy-dom tests). **`apps/extension`**: capture (selection → floating Save → anchor) → **persist to the extension-origin Dexie store** → **re-highlight on revisit** (CSS Custom Highlight API). Dexie path verified with a fake-indexeddb test (url-index query + sync-over-Dexie + tombstone). **Cloud sync + email/password auth** (background `SyncEngine`/`SupabaseRemoteAdapter`; session in `chrome.storage`; `VITE_SUPABASE_*` from root `.env`). **Image + screenshot-region capture** → `snippet-images` Storage bucket (PRD §8.4): right-click image (fetch+upload, or src-URL reference when signed out), or popup → inject a drag overlay via `chrome.scripting.executeScript` (works even when the content script isn't in the tab) → `captureVisibleTab` + `OffscreenCanvas` crop in the SW. Upload helper in `src/lib/storage.ts`. **Onboarding/empty states (PRD ON-2/ON-3, extension half):** popup shows a warm sign-in intro + a first-run "Getting started" guide (library empty) vs. a per-page empty state (library non-empty, nothing on this page) — driven by a `total` count folded into the `tessera:get-snippets` reply; teaches only shipped capture modes (text/image/screenshot + sync), not yet-unbuilt documents/AI. **Delete:** each popup row has a trash button with an inline Delete/Cancel confirm (optimistic) → `localDelete` writes a tombstone that syncs (LWW) + best-effort removes the uploaded Storage object (external `http…` srcUrl refs left alone); the content script re-paints via `tessera:refresh-highlights` so a deleted highlight clears without a reload. 24 tests green; **sync + storage** (bucket + per-user RLS) proven by smoke tests vs local Supabase (`npm run -w @tessera/db sync:smoke` / `storage:smoke`); capture/auth/onboarding UI verified manually in-Chrome. Magic-link + the landing-page half of ON-1/ON-2 deferred to the web app (M2).
- M2 (core complete): **`apps/web` scaffolded + library shipped.** Magic-link + email/password auth and the landing/empty-state web halves of ON-1/ON-3 (deferred from M1). **LIB-1** browse-by-website (grouped cards, counts, recency), **LIB-2** site→page drill-down (snippets in capture order), **LIB-3** live search (switches to a flat result list), **LIB-4** filters by type/color/date (color list derived from data) — **tag filter deferred to M3** (no tags captured yet), **LIB-5** snippet detail (full text/image, note, metadata, delete via `localDelete`+sync+best-effort image removal), **LIB-6** "open source" as a native `#:~:text=` Text Fragment built from the anchor quote, **LIB-7** newest/oldest sort (**most-referenced deferred to M3** — needs document refs). Verified end-to-end in-browser against local Supabase (signed in as the smoke user, seeded/cleaned 4 snippets): sign-in → `SyncEngine` pull → grouped library → drill-down → detail → deep-link href confirmed; empty-state + search confirmed. `npm run build|typecheck|lint` green across the workspace.
- M3 (core complete): **Documents + Notes shipped.** `documents`/`document_items`/`tags`/`snippet_tags` were already in the schema (M0), so M3 was client work: `@tessera/core` `order.ts` fractional indexing (8 tests); Dexie **v2** tables + sync wired (`createEngine` now syncs all five tables, parent-first); **`apps/web` documents** — **DOC-1** create/rename/describe/delete docs (`/documents` list + new-doc dialog), **DOC-2/3** add a snippet by reference to many docs / multi-source (add-to-document dialog on snippet detail + in-editor snippet picker), **DOC-4** drag-reorder (native HTML5 drag handle) **+ up/down buttons** (a11y), **DOC-5/NOTE-4** interleave heading/text blocks (inline-edit, auto-grow), **DOC-6** remove a ref without deleting the snippet, **DOC-7** each ref links back to source (`#:~:text=`), **DOC-8** "where used" on snippet detail; **NOTE-1** per-snippet note, **NOTE-2** tag + color editing, **NOTE-3** light text edit w/ `edited` flag (migration `20260609000000_snippet_edited.sql`, nullable so omitted-field pushes don't break). Deferred M2 bits wired: **LIB-4 tag filter** + **LIB-7 most-referenced sort**. **Sync engine hardened** (parent-first push order + per-table error isolation; 19 db tests). **Verified end-to-end in-browser against local Supabase** (smoke user): created "Cell Biology — Midterm", added Wikipedia + example.com snippets (multi-source) + a heading, reordered, tagged, noted — and confirmed the whole document **round-trips to the cloud** (outbox drains to 0; document/items/tag/join all on the server). `build`/`lint`/`typecheck`/`test` green (34 tests: 15 core + 19 db).
- M4 (export shipped): **EXP-1/2/3 done.** Markdown download (`documentToMarkdown` → slugified `.md`) + a chrome-free print-to-PDF view at `/documents/[id]/print`, both with per-snippet provenance (source deep-link + domain + saved date) and interleaved headings/notes. **Verified end-to-end in-browser** against the seeded "Cell Biology — Midterm" doc (smoke user): the **Export** menu generates a real `cell-biology-midterm.md` (920-byte `text/markdown` blob, correct block quotes + `#:~:text=` citations + notes), and the print route renders a clean `article`/`figure`/`figcaption` layout (200, toolbar `print:hidden`, quote rule tinted by the snippet's highlight color). `build`/`lint`/`typecheck`/`test` green (**47 tests: 28 core incl. 13 export + 19 db**).
- M4 (account delete + data export shipped, PRD §8.3): `/account` page — **export** all your data as portable JSON, and **delete account** (typed-confirm) which purges everything. New migration `0004` `delete_account()` (SECURITY DEFINER; deletes the auth user → every table cascade-purges); client clears Storage images + the local mirror + signs out. **Verified end-to-end against local Supabase** by a self-contained, non-destructive smoke (`npm run -w @tessera/db delete:smoke` → `account-delete-smoke.mjs`: spins up a throwaway user, seeds all five tables, calls the RPC, and a **service-role cross-check confirms every table empty + the auth user gone + credentials rejected**). Export verified in-browser (real `tessera-export-2026-06-09.json`, 4 snippets/1 doc/3 items) and the delete-confirm gating checked (no real deletion run). `build`/`lint`/`typecheck`/`test` green.
- M4 (onboarding polish shipped, ON-2/ON-3): the library home shows a live, dismissible **Getting started** checklist — capture → create a document → add a snippet to it — derived from the user's own data and hidden once complete/dismissed. **Verified in-browser**: a fresh throwaway user saw "0 of 3 done" → creating a document ticked step 2 live ("1 of 3 done") → dismiss persisted; the complete smoke user correctly sees no checklist. (Bonus: re-verified account deletion through the real UI on that throwaway — purged + signed out — then restored the smoke session.) `typecheck`/`lint` green.
- M4 (reliability pass shipped): **row-level push isolation** — a batch the server rejects for one bad row now retries row-by-row in `@tessera/db` (a poison row only blocks itself, never its siblings; `engine.test.ts` +1 → 8 engine / 20 db tests), and the web app **surfaces partial-sync state** — the sync chip turns amber "N unsynced" with the error in its tooltip + click-to-retry (`SyncEngine.pendingCount()` → `providers.tsx` → `app-shell` `SyncStatus`). **Verified in-browser** against local Supabase: injected an FK-violating `document_items` row → status went amber "1 unsynced" (the row stayed queued, nothing else starved) → removing it returned to "Synced". **M4 is complete.**
- Next: **M5 — AI study tools** (summaries → grounded Q&A with citations → flashcards → quizzes; user-selectable model, BYOK; opt-in privacy disclosure — PRD §7.7). Fast-follows: web list **virtualization** for 1000+ (PRD §8.1); a server-side Storage sweep for any account-deletion image orphans.

## Commands
`npm install`, then `npm run build|typecheck|lint|test` (workspace-wide) and `npm run format` (Prettier; docs ignored). `npm test` runs the `@tessera/db` Vitest suite. Build core before db (workspace order handles this).
- **Gotcha:** the workspace-wide `npm run build` runs `apps/web`'s `next build`, which writes the same `apps/web/.next` dir a running `next dev` uses — doing this while the dev server is up corrupts its chunk cache (`Error: Cannot find module './vendor-chunks/*.js'`). Recover by restarting dev after deleting `apps/web/.next`. Prefer per-package builds (`npm run build -w @tessera/core -w @tessera/db`) while the web dev server is running.
Backend (Docker): `npx supabase start` + `npx supabase db reset` for local dev (config in `supabase/config.toml`, analytics off to stay light); `supabase link` + `db push` for hosted. `node --env-file=.env packages/db/scripts/sync-smoke.mjs` = live end-to-end sync check.
